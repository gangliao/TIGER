let
		var x : int;
        function print ( n : int ) : int   /* integer return type */
		begin
                printi(n);
				return 10;
        end;
in
        x := print(5);					/* return value is captured in integer variable */
        return x;
end