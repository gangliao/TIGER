let
		var x : int;
        function print ( n : int ) : float   /* return type is float, this should generate return type mismatch error */
		begin
                printi(n);
				return 10.0;
        end;
in
        x := print(5);					 /* return value is captured in int variable*/
end