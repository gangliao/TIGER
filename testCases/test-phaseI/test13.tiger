/* testing nested if then else statement */
let
        var a, b, c : int := 0;
in
        if(a = b) then
			if (a > 0) then
                a := b + 2;
			else
				a := b;
            endif;
		else
				a := 2;
        endif;
        printi(a);
end