/* testing complex expression */
let
        var a, b, c, d : int := 0;
in
        if(a = b) then
                a := b + ( 5 * a) - ( 3 + c);
				else
				a := (b / 2) + (d * 3);
        endif;
        printi(a);
end