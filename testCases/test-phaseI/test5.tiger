let
        var a, b : int := 0;
        var c : float := 1.0;
in

        if(a = b) then
                a := b + (2 + a) * b + (5 / b);
				else
						  a := 2;
                                                  b := a | b;
        endif;
                c := not(a);
        printi(a);
end