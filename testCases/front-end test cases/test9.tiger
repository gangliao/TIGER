let
        var a : int := 0;
		var b : float := 0;
in
        if(a = b) then                     /* this should generate error a is int and b is float*/
                a := b + 2;
				else
				a := 2;
        endif;
        printi(a);
end