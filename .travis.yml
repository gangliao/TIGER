language: cpp

os:
  - linux
  - osx

addons:
  apt:
    packages:
      - clang-format-3.8
      - build-essential
      - automake
      - libtool
      - ccache

script:
    - make scanner
    - make parser

notifications:
  email:
    on_success: change
    on_failure: always
